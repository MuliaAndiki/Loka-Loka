import Container from "@/app/ui/container";
import { Text } from "@/app/ui/Text";

const CardProfile: React.FC = () => {
  return (
    <Container className="w-full ">
      <Text>Ini Card Profile</Text>
    </Container>
  );
};

export default CardProfile;
